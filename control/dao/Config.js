
config = {
    protocol: "http",
    host: "localhost",
    proyect: "academia",
    getUrl: () => {
        // return `${ config.protocol }://${ config.host }/${ config.proyect }/`;
        return `./`;
    }
}